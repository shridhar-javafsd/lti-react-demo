
// const fun = (arg) => {
//     console.log(arg);
// }

// const abc = 10;

// fun(abc);

const Footer = (props) => {

    return (
        <div>
            <p className="text-primary">Footer Component</p>
            {/* <p> {props.firstName} </p> */}
            {/* <p> {props.lastName} </p> */}
        </div>
    );
}

export default Footer;
